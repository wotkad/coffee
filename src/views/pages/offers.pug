extends ../layouts/master.pug

block title
  title Samba Professional | Специальные предложения

block basicSeo
  meta(name='description' content='Offers page')
  meta(name='keywords', content='offers, page')

block manifestBrowserconfigFiles
  link(href='../manifest.json', rel='manifest')
  meta(name='msapplication-config' content='../browserconfig.xml')

block content

  +header('offers') 

  include ../components/sidebar.pug
  .barba(data-barba="wrapper")
    main(data-barba="container" data-barba-namespace="offers")
      include ../components/offers.pug
    button(type="button").callback.hidden__button Оставить заявку
      .callback__counter

